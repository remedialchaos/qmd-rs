name: CD

on:
  push:
    tags: ["v*.*.*"]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  call-cd:
    # Pinned to a verified immutable revision; upstream renamed this workflow.
    uses: qntx/workflows/.github/workflows/rust-cd.yml@7b5fbf6c32e492a583b7f15e0b51d592e74f96e2
    with:
      bin: qmd
      package: qmd-cli
