name: Rust

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  call-ci:
    # Pinned to a verified immutable revision; upstream renamed this workflow.
    uses: qntx/workflows/.github/workflows/rust.yml@7b5fbf6c32e492a583b7f15e0b51d592e74f96e2