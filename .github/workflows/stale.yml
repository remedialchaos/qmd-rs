name: Close Stale Issues and PRs

on:
  schedule:
    - cron: "30 1 * * *"
  workflow_dispatch: 

jobs:
  close-stale:
    # Pinned to a verified immutable revision; upstream removed this workflow.
    uses: qntx/workflows/.github/workflows/stale.yml@7b5fbf6c32e492a583b7f15e0b51d592e74f96e2